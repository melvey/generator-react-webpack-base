/**
* <%= filename %>
* Redux Reducer for <%= name %>
**/
<% var actionUC = action.charAt(0).toUpperCase() + action.slice(1); %>
import actionTypes from '../actionTypes';

function handle<%= actionUC %>(store, data) {
  return Object.assign({}, store, {<%= action %>: data});
}

export default function set<%= name %>Reducer(state, action) {
	switch(action.type) {
		case actionTypes.<%= action %>:
			return handle<%= actionUC %>(store, action.payload);
		default:
			return state || {};
	}
}
